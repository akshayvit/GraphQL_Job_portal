type Query {
    job(id: ID!) : Job
    jobs : [Job!]
    jobs_pushed : [Job!]
}


type Mutation {
    createJob(title: String!,description:String): Job!
}

type Company {
    id: ID!
    name: String!
}

type Job {
    id:ID!
    title : String!
    company : Company!
    description: String
}